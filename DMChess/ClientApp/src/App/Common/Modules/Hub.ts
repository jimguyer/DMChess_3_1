import { GM } from '../../Main/Shared/Modules/Global';

export class Hub {
  public static Init() {
    //console.log("Hub.Init")
      //GM.Hub.start()
      //    .then(
      //        function () {
      //        }).catch(function (err) {
      //            return alert("Hub error");
      //        }
      //        );
  }
}
